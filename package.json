{
  "name": "secure-viewer-server",
  "version": "1.0.0",
  "description": "Server-side secure viewer wrapping an encrypted blob with global attempt limits.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}